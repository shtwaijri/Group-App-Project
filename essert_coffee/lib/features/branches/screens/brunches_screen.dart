import 'package:flutter/material.dart';

class BrunchesScreen extends StatelessWidget {
  const BrunchesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Brunches screen')));
  }
}
